class LoginFunction {
  final String username;
  final String password;

  const LoginFunction({
    required this.username,
    required this.password,
  });

  Future<bool> loginSystem() async {
    bool result = false;

    // login auth functions

    return result;
  }
}